Connection:
createStatement()创建向数据库发送sql的statement对象
prepareStatement(sql);创建向数据库发送预编译sql的PrepareStatement对象
prepareCall(sql):创建执行存储过程的callableStatement对象
setAutoCommit(boolean autoCommit):设置是否自动提交
commit():在链接上提交事务
rollback():在此链接上回滚事务

Statement
execute(String sql):向数据库发送sql语句
executeQuery(String sql):只能向数据库发送select语句。
executeUpdate(String sql):只能向数据库发送insert,update或delete语句
addBatch(String sql):把多条sql语句放到一个批处理中
executeBatch():清空缓冲

int i=stmt.executeUpdate(sql); //插入一条数据返回就是1,2条数据i就是2m