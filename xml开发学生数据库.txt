1.Exam.xml保存学生的数据库
2.StudentDao(Dte access Object)数据库访问对象(放在dao包)
1.add(Student)
2.find()
3.delete
与用户打交道的放在UI(用户界面user iterface)包

Student(javabean) 放在bean,domain,entity包

XmlUtils 工具类
getDocument()
write2Xml(Document doc)

先写底层的bean

dao包：放与数据库交互
domain(entity)包：放实体类（如Student）
exception包：放自定义异常包
UI包：放与用户打交道的包
utils包：静态工具类